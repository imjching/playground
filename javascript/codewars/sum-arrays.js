//http://www.codewars.com/kata/sum-arrays

// create a function which sums all the numbers in array
const sum = (arr) => arr.reduce((e, v) => e + v, 0);

// normal function
function sum (arr) {
  return arr.reduce((e, v) => e + v, 0);
}